# Contributing

Create a branch from `main`, keep changes focused, run `npm run lint` and `npm run build`, then open a pull request.

Never submit API keys, passwords, production URLs, real names, phone numbers, payment slips or other personal data.
