"use client";
import { FormEvent,useState } from "react";
export default function MemberCheckin(){const [result,setResult]=useState<any>(null);const [loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const fd=new FormData(e.currentTarget);const r=await fetch("/api/member-checkin",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(fd))});const d=await r.json();setResult(r.ok?d:{status:"error",message:d.error});setLoading(false)}return <main className="public-checkin"><section className="checkin-card"><div className="checkin-logo">F</div><span className="eyebrow">DEMO GYM GYM</span><h1>เช็กอินเข้ายิม</h1><p>กรอกเบอร์โทรที่สมัครสมาชิกและ PIN 4 หลัก<br/><small>PIN เริ่มต้นคือเลข 4 ตัวท้ายของเบอร์โทร</small></p><form onSubmit={submit}><label>เบอร์โทรศัพท์<input name="phone" inputMode="tel" required placeholder="08X-XXX-XXXX"/></label><label>PIN 4 หลัก<input name="pin" inputMode="numeric" maxLength={4} required placeholder="••••"/></label><button disabled={loading}>{loading?"กำลังตรวจสอบ…":"Check-in"}</button></form>{result&&<div className={`checkin-result ${result.status}`}><b>{result.name||"แจ้งเตือน"}</b><span>{result.message}</span></div>}<div className="privacy-note">ระบบบันทึกเฉพาะวันและเวลาเข้าใช้</div></section></main>}
